#! /usr/bin/env bash
set -o nounset


cd $WRK_DIR

echo 'Refreshing dependcies'
pnpm install
echo 'Starting program'

if [[ -x $(which kitty) ]]
then
    kitty --config ./devtools/kitty/kitty.conf
else
    tmuxinator local
fi

